import React from 'react';
import { Box, Text, Card, CardBody, Heading } from '@chakra-ui/react';

const GroupsManager: React.FC = () => {
  return (
    <Card>
      <CardBody>
        <Heading size="md" mb={4}>Groups Manager</Heading>
        <Text color="gray.600">
          Groups Manager component - TypeScript conversion in progress...
        </Text>
      </CardBody>
    </Card>
  );
};

export default GroupsManager;