import React from 'react';
import { Box, Text, Card, CardBody, Heading } from '@chakra-ui/react';

const LogViewer: React.FC = () => {
  return (
    <Card>
      <CardBody>
        <Heading size="md" mb={4}>Log Viewer</Heading>
        <Text color="gray.600">
          Log Viewer component - TypeScript conversion in progress...
        </Text>
      </CardBody>
    </Card>
  );
};

export default LogViewer;