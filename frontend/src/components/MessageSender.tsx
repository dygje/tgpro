import React from 'react';
import { Box, Text, Card, CardBody, Heading } from '@chakra-ui/react';

const MessageSender: React.FC = () => {
  return (
    <Card>
      <CardBody>
        <Heading size="md" mb={4}>Message Sender</Heading>
        <Text color="gray.600">
          Message Sender component - TypeScript conversion in progress...
        </Text>
      </CardBody>
    </Card>
  );
};

export default MessageSender;