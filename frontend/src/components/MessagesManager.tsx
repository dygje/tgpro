import React from 'react';
import { Box, Text, Card, CardBody, Heading } from '@chakra-ui/react';

const MessagesManager: React.FC = () => {
  return (
    <Card>
      <CardBody>
        <Heading size="md" mb={4}>Messages Manager</Heading>
        <Text color="gray.600">
          Messages Manager component - TypeScript conversion in progress...
        </Text>
      </CardBody>
    </Card>
  );
};

export default MessagesManager;