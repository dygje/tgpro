import React from 'react';
import { Box, Text, Card, CardBody, Heading } from '@chakra-ui/react';

const BlacklistManager: React.FC = () => {
  return (
    <Card>
      <CardBody>
        <Heading size="md" mb={4}>Blacklist Manager</Heading>
        <Text color="gray.600">
          Blacklist Manager component - TypeScript conversion in progress...
        </Text>
      </CardBody>
    </Card>
  );
};

export default BlacklistManager;